import styled from "styled-components";

import { TransparentIconButton } from "../button";

export const CheckboxItemContainer = styled.div`
  display: flex;
  align-items: center;
`;

export const CustomCheckbox = styled(TransparentIconButton)`
  padding: 0 2px;
  margin: 0 16px 0 0;
`;
